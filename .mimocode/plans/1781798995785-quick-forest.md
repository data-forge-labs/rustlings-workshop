# Plan: Add Rate Limiter Workshop to Section 10

## Overview
Create a new workshop in `10-ProductionSystems/05-RateLimiter/` that teaches gRPC rate limiting with Tonic, Redis, and sliding window algorithms. This serves as a capstone tying together traits, error handling, proptest, saturating arithmetic, async patterns, and Redis — while introducing gRPC/Tonic and rate limiting as new concepts.

## Files to Create

### 1. `10-ProductionSystems/05-RateLimiter/README.md`
Full workshop README following the established preamble format:
- H1 + test-driven banner
- `---` separator
- `## What Is This Project?` — rate limiter with sliding window
- Python equivalent (redis-py rate limiting)
- Transition line listing new concepts
- Topics covered table

Body sections:
1. Project Overview — what is rate limiting, why it matters
2. Prerequisites — links to RedisAsync, AxumAuth, Proptest
3. Concept: Rate Limiting Algorithms (fixed window, token bucket, sliding window)
4. Concept: gRPC with Tonic (protobuf, async service)
5. Concept: Redis Lua Scripts (atomic operations)
6. Concept: Clock Abstraction (trait + mock for testing)
7. Concept: Sliding Window Implementation
8. Concept: Policy System (pattern matching, priority)
9. Concept: Health Checks
10. Putting It All Together
11. Exercises
12. Summary

### 2. `10-ProductionSystems/05-RateLimiter/workshop/` (Cargo project)
- `Cargo.toml` — dependencies: tonic, prost, redis, tokio, thiserror, chrono, proptest
- `src/lib.rs` — core algorithm with progressive test stubs
- `src/main.rs` — gRPC server entry point

### 3. `10-ProductionSystems/05-RateLimiter/proto/rate_limiter.proto`
Protobuf definition for the gRPC service

## Key Concepts Covered

### New to course:
1. Rate limiting algorithms (sliding window)
2. gRPC with Tonic + protobuf
3. Clock abstraction / mock clocks
4. Lua scripts for atomic Redis operations
5. HTTP health check endpoints
6. `tokio::join!` (concurrent await)

### Review (reinforced):
- Traits (02-02-Traits)
- `thiserror` (02-03-TicketV2)
- Saturating arithmetic (01-03-BasicCalculator)
- Property-based testing (09-04-Proptest)
- Async patterns (05-13-AsyncPatterns)
- Redis integration (14-03-RedisAsync)

## Updated Files
- `10-ProductionSystems/README.md` — add Project 05 row
- `README.md` (root) — update Projects table and coverage table

## Verification
1. `cargo check` in workshop/
2. `cargo test` — all progressive tests pass
3. README follows preamble format (---, What Is This, Python equivalent, transition, Topics table)
4. All concept sections have Python comparisons
